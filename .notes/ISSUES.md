# Open issues
- The driver compiles the launches a second time some seconds into a process and swaps the code in, and the second code moves the last bit of some rays' direction although `rayAt` is `precise`: at a frame after the swap the hit distance is identical on every texel, `clipDepth` of the same direction and distance differs by an ulp on 18 texels with its arithmetic marked `precise` too, the sky's motion differs on 18% of the texels through its cancellation, and the shading on a few. On 610.57.04 with the driver's disk cache refused: `bench --views=one-cell-walk --seconds=6 --upscale=off --filter=false --hashes` against the same with `--hold=8` differed from frame 195 to 280 of the held leg in ten pairs of ten at a two-second warm-up, and a twelve-second pair on the picture alone from frame 649; `VK_PIPELINE_CREATE_DISABLE_OPTIMIZATION_BIT` on every pipeline changed nothing; twenty seconds of warm-up put the swap inside the warm-up of both legs in three pairs of three, which is what `sCodeSettleSeconds` in `apps/rtxtool/main.cpp` now asks of a compared run's first stop, a floor measured on one day and not a contract. Every history past the trace carries the swap's ulps for the rest of a run: Ray Reconstruction's picture from the swap frame on, which is the storm's two states in `shot --views=all --map`, the release walk's 288 frames from frame 913 and the guild's frame 7 in one run of three, and a measured exposure spreads it over the frame.
- `balmora-storm-night` traced after twenty seconds of `balmora-mages-guild` in one process draws its rain in different places in every run: `bench --views=balmora-mages-guild,balmora-storm-night --warmup=20 --seconds=2 --exposure=1 --upscale=off --filter=false --hashes` and then `--against` differ on `g-puffs` and `g-puffs-depth` on 120 of 120 frames of the storm, and so does the picture, with every other traced column and every scene column identical — the `sprites` column among them, so the table the frame was handed is the same and what the trace drew from it is not. Read back at one frame, 14% of the texels differ, and each differing texel is a drop one run drew and the other did not: `g-puffs` (0.010, 0.011, 0.012, 0.99) against (0, 0, 0, 1) with `g-puffs-depth` 254 units against nought, and the other way round elsewhere, while `g-direct` is identical on every texel. The guild's frames are identical, and the storm alone after twenty or eight seconds of warm-up is identical on every column. The same at the defaults, where the guild warms twenty seconds and the storm two. Measured 2026-09-19.

# Open issues
- The release build (`build-release`, `-O3 -DNDEBUG`) does not repeat itself while the debug build does. `rtx release bench --views=one-cell-walk --hashes=a; rtx release bench ... --against=a` on one binary differed on 288 of 1200 frames from frame 913 with every scene and constants column identical, and `rtx release shot --views=dagoth-ur-caldera --against=<its own earlier shot>` differed by 43 of 255 on 29% of the pixels; `rtx debug repeat --pairs=10` on the same tree was identical ten times. Measured 2026-09-19 after the sky-state change and against the pre-change stash alike.

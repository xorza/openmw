# Open issues

- `rtx <flavour> kernels` hashes the ids `spirv-opt --canonicalize-ids` gives, and that pass does
  not number two equal programs alike. Moving the specular table's lookup into four functions of
  `brdf.h` left the tuple of `visibilityhit.rchit` with every constant off the same instruction
  for instruction, types included, but renumbered some of its types and every instruction that
  names them, and the verb reported all 64 tuples of the module as moved.
